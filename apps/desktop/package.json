{
  "name": "@mimir/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri desktop shell. Wraps the @mimir/web static export with native notifications.",
  "scripts": {
    "dev": "WEBKIT_DISABLE_DMABUF_RENDERER=1 WEBKIT_DISABLE_COMPOSITING_MODE=1 tauri dev",
    "build": "tauri build",
    "typecheck": "echo \"no TypeScript entrypoints yet\" && exit 0",
    "lint": "echo \"no lint rules yet\" && exit 0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}