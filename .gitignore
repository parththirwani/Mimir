# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Per-deployment LLM config (deploy via volumes/CI, see model-config.example.json)
packages/backend-core/model-config.json

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist

# Rust/Tauri build artifacts (multi-GB)
apps/desktop/src-tauri/target/


# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem
implementation-plan-final.md
plan.md

# Intent-iteration harness results (regenerated every run)
scripts/intent-iteration/results/

# Phase 13 eval-harness run logs (regenerated every run)
results/eval-runs/

# LoCoMo dataset (download on demand — ~2.8MB, gitignored)
scripts/eval/locomo10.json

# Prisma generated client
packages/backend-core/src/generated/

# LLM prompts (deploy via volumes/CI, see prompts.example)
apps/api/src/prompts/
